public class Maths { 
    public static void main(String[] args) {
        //Math.ceil(x) return the next number of x
        //Math.floor(x) return the whole number of x 
        //Math.round(x) return the rounded number of x
        //Math.max(a,b) return the greater number
        //Math.random() returns double
        int result = (int) (Math.random() * 100) ; 
        System.out.println(result); 
    }
}